# Build output
build/
*.o
*.a
*.so

# Compiled spike harness (source is tracked, the binary is not)
spike/harness

# Editor
.vscode/
.atl/
spike/*.so

# Full-desktop probe screenshots: they capture the whole screen, including
# unrelated windows. Only the cropped panel strips are committed as evidence.
spike/out/live-*.png

# Debian build artefacts
debian/.debhelper/
debian/files
debian/*.substvars
debian/xfce4-window-button-colors/
obj-*/

# Editor workspace files
*.code-workspace
debian/debhelper-build-stamp
